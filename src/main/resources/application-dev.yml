spring:
  datasource:
#    url: jdbc:mysql://root:mrPBxHeOLLVOCxACUzldvNtkDaGZXFnb@nozomi.proxy.rlwy.net:19185/railway
    #url: jdbc:mariadb://localhost:3306/vdt_db
    url: jdbc:mariadb://localhost:3307/vdt_db
    username: root
    password: root
#    username: root
#    password: "mrPBxHeOLLVOCxACUzldvNtkDaGZXFnb"
#    password: "elijah"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration


  mail:
    host: ${EMAIL_HOST:smtp.mailtrap.io} # Environment variable for email host, default to mail.designxcel.com
    port: 2525 # Changed to 587 for TLS (or use 465 for SSL)
    username: ${EMAIL_USERNAME:094c8f37e74ad1} # Environment variable for email username
    password: ${EMAIL_PASSWORD:72fff4c84bd7ca} # Environment variable for email password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true # Ensure this is true for TLS
          ssl:
            enable: false # Set to true if using SSL (port 465)
    default-encoding: UTF-8

#    from: ${EMAIL_USERNAME}

#logging:
#  level:
#    org.springframework.security: TRACE
